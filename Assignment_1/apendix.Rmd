# Appendix

## Code
### Code used for `r Chapter01`

```{r, class.source = c("numCode", "R", "numberLines"), code = readLines('knitr/01.R'), comment = NA, echo=TRUE, eval=FALSE, tidy=TRUE, tidy.opts = list(blank = FALSE, width.cutoff = 55), }
```

### Code used for `r Chapter02`

```{r, class.source = c("numCode", "R", "numberLines"), code = readLines('knitr/02.R'), comment = NA, echo=TRUE, eval=FALSE, tidy=TRUE, tidy.opts = list(blank = FALSE, width.cutoff = 55)}
```
